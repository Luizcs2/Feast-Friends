# This is an example file. Copy it to .env and fill in the actual values.
# DO NOT commit the .env file with real secrets to version control.

# Application Configuration
# -------------------------
# The port your web server will listen on.
APP_PORT=8080
# The mode for the Gin framework ('debug' for development, 'release' for production).
GIN_MODE=release

# Database Configuration (Example for PostgreSQL)
# ------------------------------------------------
# The hostname of the database server. When using Docker Compose, this is the service name (e.g., 'db').
DB_HOST=localhost
# The port the database is running on.
DB_PORT=5432
# The username for connecting to the database.
DB_USER=postgres
# The password for the database user.
DB_PASSWORD=your_db_password
# The name of the database to use.
DB_NAME=feast_friends
# SSL mode for the database connection ('disable' is common for local dev).
DB_SSL_MODE=disable

# External Service Keys (if any)
# ----------------------------------
# Example for an API key you might need to connect to another service.
# EXTERNAL_API_KEY=